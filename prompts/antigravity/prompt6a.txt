Fix domain-specific course routing and course resolution issues in the LearnPath Courses Intelligence System.

IMPORTANT:
Before making any changes, study ONLY the existing Courses system architecture and align with the current LearnPath ecosystem already implemented:

* Dashboard
* Adaptive Roadmap Intelligence Engine
* Assessments Engine
* AI Mentor
* Courses Intelligence System

Maintain COMPLETE consistency with:

* existing routing patterns
* existing config-driven architecture
* current engineering-domain abstraction
* current folder structure
* current course data structure
* current visual/UI systems

DO NOT redesign UI.
DO NOT regenerate pages.
DO NOT modify layouts.
DO NOT touch styling.
DO NOT create new systems.
DO NOT refactor unrelated files.
DO NOT regenerate mock datasets.

This task is STRICTLY:
a lightweight routing + resolution stabilization/debugging task.

==================================================
CURRENT ISSUE
=============

Some engineering domains (example: Civil, Robotics, etc.) incorrectly redirect to the default Computer Engineering course:

* /courses/cs-dsa-mastery

This indicates the course routing or course lookup system is incorrectly:

* falling back to a default CS course
* reusing a shared slug
* ignoring domain-aware course resolution
* using incorrect fallback behavior
* resolving the wrong workspace data

==================================================
ONLY DEBUG AND FIX
==================

ONLY investigate and fix:

* course slug generation
* route resolution
* domain-course mapping
* course lookup logic
* fallback behavior
* dynamic workspace loading
* slug uniqueness
* route param handling

Investigate ONLY these files if needed:

* engineeringCourses.ts
* courseCatalog.ts
* moduleConfigs.ts
* CourseWorkspace.tsx
* CoursesPage.tsx

Also inspect:

* route param resolution
* slug matching logic
* domain filtering logic
* default fallback course behavior

==================================================
REQUIREMENTS
============

Requirements:

* every engineering domain must open its OWN correct course workspace
* domain-specific courses must resolve correctly
* no incorrect fallback to cs-dsa-mastery
* routing must remain config-driven
* maintain current architecture consistency
* preserve existing UI exactly as-is
* preserve existing data/config architecture

Examples:

* Civil course → Civil workspace
* Robotics course → Robotics workspace
* Mechanical course → Mechanical workspace

NOT:
all routes → CS workspace.

==================================================
IMPORTANT IMPLEMENTATION RULES
==============================

Use MINIMAL changes only.

Avoid:

* unnecessary refactors
* route rewrites
* giant file rewrites
* redesigns
* architecture restructuring
* token-heavy generation

Prefer:

* small targeted fixes
* deterministic routing logic
* lightweight corrections
* reusable route resolution
* preserving existing patterns

==================================================
LIKELY ROOT CAUSES TO CHECK
===========================

Check for:

* reused slugs across domains
* static fallback values
* hardcoded default course IDs
* incorrect useParams handling
* missing domain-aware filtering
* fallback logic always resolving to cs-dsa-mastery
* invalid route matching
* shared workspace loader assumptions

==================================================
GOAL
====

Fix domain-aware course navigation and ensure correct course workspace loading for ALL engineering branches while preserving the existing LearnPath architecture, UI consistency, and config-driven system design.

This is a lightweight debugging/stabilization task — NOT a regeneration task.
