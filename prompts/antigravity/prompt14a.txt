Apply these mandatory corrections before execution.

---

1. Controller Layer
   Do NOT change AssessmentController responsibilities. Keep it thin.

Flow must be:
AssessmentController → AssessmentService.submitSession() → AssessmentEvaluationService.evaluateSession() → AssessmentCompletedEvent → SkillEventListener → ProgressionEventListener

Controller must not invoke evaluation logic directly.
AssessmentService remains the orchestration entry point.

---

2. Idempotency (Service + DB)

Service layer must prevent duplicate evaluation by checking:

* session already evaluated
* result exists
* metrics exist

Database must enforce uniqueness:

* assessment_results.session_id UNIQUE
* assessment_metrics.assessment_result_id UNIQUE

Add Flyway migration if missing.
Duplicates must be impossible even under concurrency.

---

3. Event Safety

AssessmentCompletedEvent must run only AFTER successful commit.

Use:
@TransactionalEventListener(AFTER_COMMIT) or equivalent.

Required sequence:
Persist Result → Persist Metrics → COMMIT → Publish Event → Skill Update → Progression Update

Event must NOT fire on rollback.

---

4. Architecture Boundaries

Assessment module: create results, metrics, publish event
Skill module: listen + update user_skills
Progression module: listen + update user_progression

No direct Assessment → Skill/Progression service calls.
Event-driven only.

---

Revalidate plan with these constraints.
If compliant, proceed with Phase 3B execution only.

Do NOT introduce new architecture or Phase 4 modules.
