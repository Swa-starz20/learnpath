Fix assessment navigation and accessibility issues in the LearnPath Assessments Engine.

IMPORTANT:
Before making changes, study ONLY the existing Assessments system implementation and align with the already-built LearnPath ecosystem:

* Dashboard
* Adaptive Roadmap Intelligence Engine
* AI Mentor
* Courses Intelligence System
* Shared Intelligence Infrastructure

Maintain COMPLETE consistency with:

* current sidebar/navigation architecture
* current routing patterns
* current glassmorphism UI system
* current Framer Motion interactions
* existing component hierarchy
* current design language from Stitch
* current assessment page structure

DO NOT redesign UI.
DO NOT regenerate pages.
DO NOT modify layouts.
DO NOT touch styling unnecessarily.
DO NOT create new systems.
DO NOT refactor unrelated files.
DO NOT alter assessment logic or scoring systems.

This task is STRICTLY:
a lightweight navigation/accessibility fix.

==================================================
CURRENT ISSUE
=============

Inside the Assessments section:

* Personality
* Aptitude
* Behavioral
* Results

appear visually in the sidebar/submenu,
BUT clicking them does NOT navigate properly.

Currently, users must manually type URLs such as:

* /assessments/personality
* /assessments/aptitude

to access those pages.

This breaks:

* user navigation flow
* UX consistency
* accessibility
* routing continuity

==================================================
ONLY DEBUG AND FIX
==================

ONLY investigate and fix:

* submenu click handling
* sidebar navigation wiring
* route linking
* nested route navigation
* active tab navigation
* route path binding
* NavLink/Link integration
* assessment submenu interaction logic

Investigate ONLY relevant files if needed:

* Sidebar.tsx
* Assessments navigation component
* assessment routes
* router configuration
* App routing setup
* assessment submenu rendering logic

==================================================
REQUIREMENTS
============

Requirements:

* users must be able to navigate by clicking submenu items
* Personality click → opens Personality assessment
* Aptitude click → opens Aptitude assessment
* Behavioral click → opens Behavioral assessment
* Results click → opens Results page
* preserve existing UI exactly
* preserve current animations
* preserve current styling
* preserve current sidebar structure
* maintain config-driven architecture

==================================================
IMPORTANT IMPLEMENTATION RULES
==============================

Use MINIMAL changes only.

Avoid:

* navigation redesigns
* sidebar rewrites
* routing architecture rewrites
* large refactors
* token-heavy generation
* unnecessary component regeneration

Prefer:

* lightweight fixes
* proper NavLink integration
* route-aware submenu mapping
* reusable route binding
* preserving existing structure

==================================================
LIKELY ROOT CAUSES TO CHECK
===========================

Check for:

* submenu items rendered as static divs instead of links
* missing onClick handlers
* incorrect route paths
* missing nested routes
* inactive NavLink integration
* broken route mapping
* sidebar state preventing navigation
* missing router bindings

==================================================
GOAL
====

Fix Assessments submenu navigation so users can access all assessment sections directly through the UI without manually editing URLs, while preserving the existing LearnPath architecture, UI consistency, and current design system.

This is a lightweight navigation stabilization task — NOT a redesign task.
