Integrate the existing Adaptive Intelligence Behavior Layer into the LearnPath ecosystem.

IMPORTANT

The adaptive layer already exists and is production-ready:

/src/engine/

* adaptiveEngine.ts
* difficultyEngine.ts
* behaviorEngine.ts
* mentorEngine.ts

DO NOT recreate, replace, or refactor these files unless required for integration.

DO NOT redesign UI.

DO NOT create pages.

DO NOT modify layouts.

DO NOT touch styling.

DO NOT create parallel systems.

DO NOT duplicate intelligence logic.

This is a wiring and orchestration task only.

The adaptive layer exists but is currently disconnected from the active intelligence pipeline.

The following systems are already integrated with the Unified Intelligence Sync Engine:

* Dashboard
* Assessments
* Courses
* Roadmap
* AI Mentor Context

The missing step is connecting adaptive behavior outputs to the existing intelligence lifecycle.

Make adaptive behavior a first-class participant in the platform intelligence flow.

Desired flow:

Assessment / Course / Roadmap Event
↓
Intelligence Sync Engine
↓
Adaptive Engine
↓
Difficulty Decision
↓
Behavior Classification
↓
Mentor Guidance
↓
Path Adjustments
↓
Dashboard / Mentor / Roadmap Consumers

Integrate adaptiveEngine into:

/src/intelligence/intelligenceSyncEngine.ts

Inside:

runIntelligenceCycle()

Execute:

runAdaptiveCycle()

immediately after recommendation generation and before downstream consumers are updated.

Reuse existing intelligence state.

Reuse existing recommendation outputs.

Do not duplicate calculations, recommendations, progression analysis, or learner profiling.

AI Mentor Integration

Consume adaptive mentor outputs.

Use:

* mentorContext
* behaviorPattern
* guidanceBundle

Prefer extending existing mentor context structures rather than creating new ones.

Ensure adaptive guidance becomes available to mentor intelligence consumers.

Do not alter chat UI.

Do not create mentor-specific interfaces.

Dashboard Integration

Consume adaptive state where appropriate.

Examples:

* readiness trend
* behavior pattern
* adaptive priority
* mentor focus

Reuse existing dashboard intelligence cards and data pipelines whenever possible.

No new UI sections.

No layout changes.

No visual modifications.

Roadmap Integration

Consume:

pathAdjustments

Use:

* targetNodeIds
* targetSkills
* roadmap pacing suggestions

Wire adaptive outputs into existing roadmap intelligence and progression flows.

Only connect data.

Do not redesign roadmap UI.

Do not create roadmap-specific adaptive systems.

Difficulty Integration

If adaptive state indicates:

increase
decrease
maintain

ensure the decision is exposed through the existing intelligence state and available to current consumers.

Reuse existing state management.

Do not create new controls.

Do not create new difficulty workflows.

Verify:

* adaptive cycle executes after recommendation generation
* adaptive cycle participates in the intelligence lifecycle
* mentor receives adaptive guidance
* dashboard receives adaptive intelligence
* roadmap receives adaptive path adjustments
* difficulty decisions are exposed through intelligence state
* no duplicate recommendation logic
* no duplicate progression logic
* no duplicate learner analysis
* no TypeScript errors
* no architecture regressions

Provide ONLY:

1. Files modified
2. Adaptive functions integrated
3. Consumers updated
4. Validation results
5. Remaining adaptive gaps

Keep changes minimal.

Reuse existing architecture.

Prefer composition over new implementation.

Do not generate UI.

Do not redesign anything.

Do not create new systems.

Do not introduce redundant state, services, or intelligence pipelines.
