Integrate the existing Unified Intelligence Sync Engine into the current LearnPath ecosystem.

IMPORTANT

The intelligence infrastructure already exists and MUST be reused:

/src/intelligence/

* intelligenceEvents.ts
* intelligenceSyncEngine.ts
* intelligenceSelectors.ts
* userIntelligenceState.ts

DO NOT recreate, redesign, duplicate, or replace these files.

This is an integration task only.

Perform ONLY the minimum targeted changes required to connect the existing intelligence layer to the current platform.

Target integration points include:

* main.tsx
* App.tsx
* DashboardPage.tsx
* RoadmapPage.tsx
* CoursesPage.tsx
* CourseWorkspace.tsx
* AI Mentor page
* Assessment pages
* useLearningStore.ts

Preserve:

* existing UI
* existing routes
* existing architecture
* existing component hierarchy
* existing design language
* existing business logic

DO NOT:

* redesign UI
* regenerate pages
* modify layouts
* touch styling
* create new systems
* perform large refactors
* rewrite existing logic
* duplicate functionality
* introduce parallel state management

==================================================

1. APPLICATION STARTUP
   ==================================================

Initialize:

* initIntelligenceSyncEngine()

Requirements:

* initialize once only
* prevent duplicate listeners
* prevent render loops
* reuse existing application startup flow

Using existing assessment completion flows:

Dispatch:

* ASSESSMENT_COMPLETED

Do not alter assessment behavior or UX.

Using existing completion actions:

When a lesson completes:

* dispatch LESSON_COMPLETED

When a lab completes:

* dispatch LAB_COMPLETED

Reuse existing completion logic wherever available.

Using existing roadmap progression logic:

When a node unlocks:

* dispatch ROADMAP_NODE_UNLOCKED

When a node completes:

* dispatch ROADMAP_NODE_COMPLETED

Do not modify roadmap behavior.

Wire existing intelligence selectors into current dashboard data sources where appropriate.

Consume:

* getCurrentReadiness()
* getDashboardSummary()
* getLearningVelocitySummary()

Replace hardcoded intelligence-related values only when safe.

Do not alter visuals, layout, or component structure.

Consume intelligence-derived context:

* mentorInsights
* recommendations
* riskAreas
* learningVelocity

Use existing AI Mentor flows and UI.

Do not redesign chat behavior, prompts, layout, or styling.

Verify:

Assessment completion
→ updates intelligence state

Lesson completion
→ updates intelligence state

Lab completion
→ updates intelligence state

Course progress
→ updates recommendations

Roadmap progression
→ updates readiness

Dashboard
→ reflects intelligence-derived values

AI Mentor
→ receives updated intelligence context

After integration provide ONLY:

1. Exact files modified
2. Events connected
3. Selectors consumed
4. Systems integrated
5. Validation results
6. Remaining integration gaps

Keep implementation minimal.

Reuse existing infrastructure.

Avoid duplicate implementations.

Avoid unnecessary code generation.

Focus strictly on wiring existing systems together.
