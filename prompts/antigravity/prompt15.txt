PHASE 4 — MENTOR SYSTEM (DIVIDED EXECUTION FOR TOKEN EFFICIENCY)

Context

Current verified state:

* backend-phase3b-stable
* Phase 1 verified
* Phase 2 verified
* Phase 3A verified
* Phase 3B verified

Current capabilities:

Assessment
→ Evaluation
→ Metrics
→ Skill Updates
→ Progression Updates

All tests passing.
Flyway V1–V9 applied successfully.

IMPORTANT EXECUTION INSTRUCTION

This is Phase 4 of the system, but it is intentionally divided into smaller sub-phases for token efficiency and architectural stability.

Antigravity / Gemini MUST treat this as:

* Phase 4A → Mentor Foundation (execute now)
* Phase 4B → Mentor Intelligence (DO NOT execute yet)

Only implement Phase 4A in this run.

Do NOT proceed to Phase 4B automatically.

If errors, bugs, or inconsistencies occur during implementation, you MUST:

* Diagnose root cause
* Fix autonomously
* Ensure compilation and tests pass
* Maintain architectural constraints

Do not leave broken code.

---

## PHASE 4A — MENTOR FOUNDATION (EXECUTE THIS ONLY)

Goal

Create the Mentor Foundation module.

This phase establishes mentor persistence, APIs, event contracts, and storage structures.

This phase MUST NOT generate mentor intelligence.

This phase MUST NOT generate recommendations automatically.

This phase MUST NOT create AI logic.

This phase MUST NOT create adaptive learning.

This phase MUST NOT create placement logic.

Mentor data structures only.

---

## DATABASE

Create Flyway migration:

V10__create_mentor_tables.sql

Create tables:

mentor_profiles

Columns:

* id UUID PK
* user_id UUID UNIQUE
* primary_domain_id UUID
* current_level VARCHAR(50)
* target_role VARCHAR(200)
* created_at TIMESTAMP
* updated_at TIMESTAMP

mentor_insights

Columns:

* id UUID PK
* user_id UUID
* insight_type VARCHAR(100)
* title VARCHAR(255)
* description TEXT
* priority INTEGER
* generated_at TIMESTAMP

mentor_recommendations

Columns:

* id UUID PK
* user_id UUID
* recommendation_type VARCHAR(100)
* title VARCHAR(255)
* description TEXT
* action_url VARCHAR(500)
* is_completed BOOLEAN
* created_at TIMESTAMP

Add indexes.

Use UUIDs.

Follow existing migration conventions.

---

## MODULE

Create module:

mentor

Structure:

mentor/
├── entity
├── repository
├── dto
├── service
├── controller

Follow existing project conventions.

Constructor injection only.

No field injection.

---

## ENTITIES

Create:

MentorProfile
MentorInsight
MentorRecommendation

No cross-module JPA relationships.

Store foreign references as UUID only.

Follow existing architecture rules.

---

## REPOSITORIES

Create repositories for all mentor entities.

Use Spring Data JPA.

---

## DTOs

Create DTOs:

MentorProfileResponse
MentorInsightResponse
MentorRecommendationResponse
UpdateMentorProfileRequest

Use validation annotations.

---

## SERVICE

Create:

MentorService

Responsibilities:

* get mentor profile
* create mentor profile
* update mentor profile
* list mentor insights
* list mentor recommendations

NO generation logic.

NO recommendation engine.

Persistence only.

---

## CONTROLLER

Base path:

/api/v1/mentor

Endpoints:

GET /api/v1/mentor/profile
POST /api/v1/mentor/profile
PUT /api/v1/mentor/profile
GET /api/v1/mentor/insights
GET /api/v1/mentor/recommendations

Authentication required.

User identity from JWT only.

---

## SECURITY

Update SecurityConfig.

All mentor endpoints require authentication.

No public mentor endpoints.

---

## TESTING

Create:

MentorControllerIntegrationTest

Verify:

* authenticated access works
* unauthenticated access returns 401
* profile create/update works
* insights retrieval works
* recommendations retrieval works

---

## ARCHITECTURE CONSTRAINTS

Do NOT create:

* mentor intelligence generation
* mentor recommendation generation
* AI services
* intelligence module
* placement module
* adaptive engine

Do NOT subscribe to AssessmentCompletedEvent yet.

Do NOT publish any mentor events yet.

This phase is storage and API foundation only.

---

## AUTONOMOUS ERROR HANDLING

If any of the following occur:

* compilation errors
* missing beans
* circular dependencies
* Flyway migration failures
* test failures

You MUST:

1. Identify the root cause
2. Fix the issue
3. Re-run validation mentally
4. Ensure system stability

Do not stop at partial implementation.

---

## VALIDATION

Run conceptually:

mvn clean compile
mvn clean test
mvn spring-boot

Verify:

* Flyway V10 applies
* no bean failures
* no circular dependencies
* all tests pass

---

## OUTPUT FORMAT

Output only:

1. Files created
2. Files modified
3. Database changes
4. API endpoints
5. Validation results
6. Remaining blockers

If successful, end with:

PHASE 4A MENTOR FOUNDATION COMPLETE

Do not generate Phase 4B.
Do not generate intelligence logic.
Do not create placement functionality.
