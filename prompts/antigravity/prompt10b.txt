Update learnpath_backend_architecture.md using the approved audit findings.

Apply ONLY these corrections:

1. Remove intelligence -> analytics dependency.
   Intelligence must read XP trend from ProgressionService only.
   Analytics becomes read-only reporting.

2. Replace mentor/intelligence direct persistence coupling with Spring Application Events.
   Intelligence emits MentorInsightGeneratedEvent.
   Mentor module persists insights.

3. Replace assessment -> skills direct dependency with Application Events.
   Assessment emits AssessmentCompletedEvent.
   Skills module listens and updates skill confidence.

4. Change roadmap_node_skills:

   * remove skill_label
   * add skill_id FK -> skills.id

5. Change adaptive path adjustment storage:
   TEXT -> JSONB

6. Change placement_profiles uniqueness:
   UNIQUE(user_id)
   ->
   UNIQUE(user_id, domain_id)

7. Add completion_records UNIQUE(user_id, entity_id)

8. Add persistence for careerAlignment:

   * career_alignment_score
   * missing_skill_ids JSONB
   * career_estimated_weeks
   * alignment_computed_at

9. Remove duplicate mastery_score ownership.
   user_progression remains sole owner.

10. Document:
    @Transactional
    on IntelligenceCycleService.runCycle()

Output ONLY:

1. Applied Corrections Summary
2. Architecture Status

If all corrections are successfully incorporated:

ARCHITECTURE APPROVED FOR IMPLEMENTATION

Do not generate code.
Do not generate entities.
Do not modify module count.
Do not redesign architecture.
