Implement **Phase 7B — Notification Intelligence**.

---

# CORE DIRECTIVE

Implement Phase 7B using the existing architecture without breaking APIs or duplicating logic.

* Follow all established patterns (entities, services, repositories, DTOs, security, transactions).
* Do not modify previous modules unless strictly required.
* No placeholders, TODOs, or incomplete code.
* Notifications must remain a reusable, event-driven infrastructure module.

---

# AUTONOMOUS EXECUTION

You must fully self-resolve all issues before completion:

* compilation errors
* Flyway migration failures
* Hibernate/JPA mapping issues
* repository/query errors
* transaction boundary issues
* security/JWT issues
* integration test failures
* event ordering/duplication issues
* runtime exceptions

Continuously fix → rebuild → retest until:

* `mvn clean compile` = SUCCESS
* `mvn clean test` = SUCCESS

Do not stop early. Do not ask questions.

---

# OBJECTIVE

Build Notification Intelligence that generates notifications from existing domain events.

Constraints:

* Consume existing events only (no new business logic duplication).
* All notifications must go through:
  `NotificationService.createNotification(...)`
* Event listeners must NEVER persist directly.

---

# DATABASE

Create migration:

**V17__notification_delivery_tracking.sql**

Table: `notification_delivery_log`

Fields:

* id
* notification_id (FK → notifications.id)
* delivery_channel
* delivery_status
* delivered_at
* failure_reason
* created_at

Requirements:

* proper indexes
* FK constraints
* timestamps + trigger if needed

No changes to existing tables unless unavoidable.

---

# ENTITY LAYER

Create:

* NotificationDeliveryLog
* DeliveryStatus (enum)
* DeliveryChannel (enum)

---

# REPOSITORY

Create:

* NotificationDeliveryLogRepository

---

# DTO

Create:

* NotificationDeliveryLogResponse

---

# EVENT LISTENERS

Use:

* `@TransactionalEventListener(AFTER_COMMIT)`
* `@Transactional(REQUIRES_NEW)`

Consume existing events:

### AssessmentCompletedEvent

Generate:

* assessment completed
* score available
* roadmap unlocked (if applicable)

### Mentor Event (existing closest match)

Generate:

* mentor insights available
* recommendations available

### Placement Event (existing closest match)

Generate:

* readiness updated
* company fit updated
* skill gap identified

### AnalyticsGeneratedEvent

Generate:

* analytics refreshed
* progress improved
* prediction available
* streak milestone

Listeners must be plug-and-play (no future service changes required).

---

# NOTIFICATION RULES

* Prevent duplicate unread notifications
* Enforce user preferences
* Skip disabled categories/channels
* Always use NotificationService
* Log every successful delivery in `notification_delivery_log`

---

# SERVICE LAYER

Create:

NotificationIntelligenceService

Responsibilities:

* event → notification mapping
* duplicate prevention
* preference filtering
* delivery logging
* reusable helpers

---

# CONTROLLER

Extend NotificationController:

GET `/api/v1/notifications/delivery-log`

* returns delivery history
* JWT secured

---

# TESTING

Create:

* NotificationIntelligenceIntegrationTest
* NotificationDeliveryLogTest

Verify:

* event listeners trigger correctly
* duplicates prevented
* preferences enforced
* delivery logs created
* transactions correct
* JWT security enforced

Run:

* `mvn clean compile`
* `mvn clean test`

---

# VALIDATION

Before completion ensure:

* BUILD SUCCESS
* All tests pass
* Flyway migrated to V17
* `notification_delivery_log` exists
* Existing APIs unchanged
* No regressions
* No duplicate notifications
* Delivery logs correct

---

# OUTPUT

Return ONLY:

1. Files Created

2. Files Modified

3. Database Changes

4. Events Consumed

5. Validation Results

6. Remaining Blockers

---

# FINAL INSTRUCTION

Complete end-to-end.

Continuously detect and fix all errors until everything compiles, tests pass, and the system is stable.

Return only after full success.
