PHASE 3A — ASSESSMENT FOUNDATION (AUTONOMOUS EXECUTION MODE)

Mission

Extend the existing backend by implementing the Assessment Foundation module only.

You are operating as a senior architect, backend engineer, database engineer, QA engineer, and build engineer simultaneously.

Your objective is to deliver a fully compiling, fully integrated, production-ready Phase 3A implementation with zero manual intervention required.

You must autonomously:

* Analyze the existing codebase before making changes
* Reuse existing architectural patterns
* Detect and resolve compilation issues
* Detect and resolve dependency issues
* Detect and resolve Flyway issues
* Detect and resolve bean wiring issues
* Detect and resolve transaction issues
* Detect and resolve validation issues
* Detect and resolve repository/query issues
* Detect and resolve package/import issues
* Detect and resolve startup failures
* Detect and resolve test failures
* Refactor only when necessary for stability
* Preserve backward compatibility

Current Verified State

* backend-phase2-stable tag exists
* Phase 1 verified
* Phase 2 verified against PostgreSQL
* Flyway V1–V7 applied successfully
* Database: learnpath
* Spring Boot startup verified
* Existing tests passing
* Architecture approved

Existing Modules

* auth
* user
* security
* domain
* skill
* roadmap
* course
* progression

Compatibility Requirement

Do not break existing functionality.

Do not modify Phase 1 or Phase 2 behavior unless required to:

* compile successfully
* satisfy dependency requirements
* fix discovered defects
* maintain architectural consistency

Any required fixes must be minimal, safe, and backward compatible.

Scope

Implement Assessment Foundation only.

Explicitly exclude:

* mentor
* intelligence
* placement
* adaptive scoring
* recommendation engines
* psychometric evaluation
* AI logic
* ML logic
* event-driven processing
* progression updates
* skill updates

This phase is strictly:

* persistence
* domain model
* repositories
* services
* REST APIs
* validation
* integration

Module

Create:

assessment

Package Structure

assessment/
├── controller
├── dto
├── entity
├── repository
└── service

Database

Create Flyway migrations after V7.

Use PostgreSQL-compatible SQL only.

Requirements

* UUID primary keys everywhere
* Explicit foreign keys
* Explicit indexes
* Audit timestamps
* Flyway-managed schema only
* No Hibernate schema generation
* Proper constraints
* Referential integrity
* Production-safe migration ordering

Tables

1. assessment_templates

* id UUID PK
* code UNIQUE
* name
* description
* assessment_type
* domain_id FK → domains
* duration_minutes
* total_questions
* is_active
* created_at
* updated_at

2. assessment_questions

* id UUID PK
* template_id FK
* question_text
* question_type
* difficulty_level
* skill_id FK → skills
* display_order
* created_at
* updated_at

3. assessment_options

* id UUID PK
* question_id FK
* option_text
* option_value
* display_order

4. assessment_sessions

* id UUID PK
* user_id UUID
* template_id FK
* status
* started_at
* submitted_at
* time_spent_seconds

5. assessment_answers

* id UUID PK
* session_id FK
* question_id FK
* selected_option_id FK NULL
* answer_text NULL
* answered_at

6. assessment_results

* id UUID PK
* session_id UNIQUE FK
* user_id UUID
* template_id FK
* score_percentage
* completed_at

Java Layer

Create:

* Entities
* Repositories
* DTOs
* Services
* Controllers

Implementation Standards

* Constructor injection only
* No field injection
* Bean Validation
* Global exception handling integration
* Clean architecture alignment
* Consistent naming conventions
* Existing project standards must be followed

Transactions

Writes:

@Transactional

Reads:

@Transactional(readOnly = true)

REST APIs

Public

GET /api/v1/assessments/templates

GET /api/v1/assessments/templates/{id}

Authenticated

POST /api/v1/assessments/sessions/start

GET /api/v1/assessments/sessions/{id}

POST /api/v1/assessments/sessions/{id}/answer

POST /api/v1/assessments/sessions/{id}/submit

GET /api/v1/assessments/results/{id}

GET /api/v1/assessments/my-results

Business Rules

* One active session per template per user
* Cannot answer after submission
* Cannot submit twice
* Result created exactly once
* No scoring engine yet
* score_percentage must default to 0
* No progression updates
* No skill updates
* No events
* No async processing

Autonomous Quality Gate

Before finalizing, perform a full implementation review and automatically fix any discovered issues.

Mandatory checks:

Architecture

* package consistency
* dependency consistency
* naming consistency
* DTO/entity separation
* repository correctness

Database

* migration ordering
* FK validity
* index coverage
* constraint correctness
* PostgreSQL compatibility

Application

* bean creation
* dependency injection
* transaction boundaries
* validation annotations
* exception handling

Build

* imports
* generics
* Lombok usage consistency
* mapping correctness
* repository method correctness

Testing & Runtime

Conceptually execute and resolve all issues until clean:

mvn clean compile

mvn clean test

mvn spring-boot

Continue fixing issues autonomously until all checks pass or no further actionable fixes exist.

Success Criteria

Verify:

* no compilation errors
* no startup failures
* no Flyway failures
* no bean creation failures
* no circular dependencies
* no repository initialization failures
* no transaction configuration issues
* no validation configuration issues
* existing tests remain green
* Phase 1 functionality intact
* Phase 2 functionality intact
* Assessment module integrated successfully

Output Format

Return only:

1. Files created
2. Files modified
3. Database tables created
4. API endpoints added
5. Validation results
6. Issues detected and automatically resolved
7. Remaining blockers (if any)

If there are no blockers, explicitly state:

Remaining blockers: None

Finish with:

PHASE 3A ASSESSMENT FOUNDATION COMPLETE

Do not generate Phase 3B.

Do not create mentor, intelligence, placement, AI, recommendation, or adaptive assessment modules.
