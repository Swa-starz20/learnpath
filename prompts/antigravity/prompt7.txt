LearnPath AI — Unified Intelligence Sync Engine

IMPORTANT

Before making any changes, study and align with the existing LearnPath architecture already implemented:

COMPLETED SYSTEMS:
- Dashboard
- Assessments Engine
- Adaptive Roadmap Intelligence Engine
- Courses Intelligence System
- AI Mentor
- Shared Intelligence Infrastructure

The platform is designed for ALL engineering domains, not only Computer Engineering.

Supported domains include:
- Computer Engineering
- AI/ML
- Robotics
- Mechanical
- Electrical
- Electronics
- Civil
- Aerospace
- Biomedical
- Automobile
- Chemical
- Mechatronics

Maintain COMPLETE consistency with:

- existing folder structure
- existing intelligence engines
- existing type system
- existing store architecture
- existing config-driven approach
- existing engineering-domain abstraction
- existing naming conventions
- existing data flow patterns

DO NOT:
- redesign UI
- create pages
- modify layouts
- touch styling
- regenerate components
- rebuild existing systems
- duplicate business logic
- introduce new state libraries

This is an ARCHITECTURE task only.

--------------------------------------------------
OBJECTIVE
--------------------------------------------------

Build the missing orchestration layer that connects all existing systems together.

Currently:

Assessments
Roadmap
Courses
AI Mentor
Dashboard

all exist and function individually.

The missing capability is:

cross-system intelligence synchronization.

A change in one system should automatically propagate intelligence updates across the rest of the platform.

--------------------------------------------------
CREATE
--------------------------------------------------

Create:

/src/intelligence/

Files:

- intelligenceSyncEngine.ts
- userIntelligenceState.ts
- intelligenceEvents.ts
- intelligenceSelectors.ts

--------------------------------------------------
1. userIntelligenceState.ts
--------------------------------------------------

Create a unified intelligence state model.

Purpose:

Single source of truth for adaptive platform intelligence.

Reuse existing types wherever possible.

Do NOT duplicate types already available inside:

- types/
- engine/
- store/

Include only high-level state slices such as:

- assessments
- roadmap
- courses
- skills
- progression
- recommendations
- mentorInsights
- readiness
- learningVelocity
- careerAlignment

Keep it lightweight and reusable.

--------------------------------------------------
2. intelligenceEvents.ts
--------------------------------------------------

Create lightweight typed platform events.

Examples:

- ASSESSMENT_COMPLETED
- LESSON_COMPLETED
- LAB_COMPLETED
- ROADMAP_NODE_UNLOCKED
- XP_GAINED
- STREAK_UPDATED
- DOMAIN_CHANGED
- TRACK_CHANGED

Provide strongly typed payload definitions.

No event library.

No external dependencies.

--------------------------------------------------
3. intelligenceSelectors.ts
--------------------------------------------------

Create reusable derived intelligence selectors.

Examples:

- getCurrentReadiness()
- getWeakSkills()
- getStrongSkills()
- getNextRecommendedCourses()
- getNextRecommendedRoadmapNodes()
- getRoadmapRiskAreas()
- getCareerAlignment()
- getLearningVelocitySummary()

Requirements:

- pure functions
- reusable
- domain agnostic
- no UI dependencies

--------------------------------------------------
4. intelligenceSyncEngine.ts
--------------------------------------------------

Create the orchestration layer.

Responsibilities:

- syncAssessmentState()
- syncRoadmapState()
- syncCourseState()
- syncMentorState()
- syncDashboardState()
- runIntelligenceCycle()

The engine must consume outputs from existing infrastructure:

- skillGraphEngine
- progressionEngine
- recommendationEngine
- sync utilities
- useLearningStore

Reuse existing logic.

Do NOT reimplement logic that already exists.

--------------------------------------------------
INTEGRATION REQUIREMENTS
--------------------------------------------------

Connect existing systems through intelligence events.

Expected flow:

Assessment Completed
      ↓
Intelligence Event
      ↓
Sync Engine
      ↓
Skill Graph Update
      ↓
Progression Update
      ↓
Recommendation Update
      ↓
Roadmap Update
      ↓
Course Update
      ↓
AI Mentor Update
      ↓
Dashboard Update

Only connect data flow.

Do NOT modify UI.

--------------------------------------------------
IMPLEMENTATION RULES
--------------------------------------------------

Use minimal changes.

Prefer:
- reusable architecture
- composable functions
- strong typing
- shared intelligence
- config-driven design

Avoid:
- large refactors
- architecture rewrites
- UI generation
- duplicate state
- hardcoded domain logic

--------------------------------------------------
SUCCESS CRITERIA
--------------------------------------------------

After implementation:

1. Assessment changes can propagate intelligence updates.
2. Course completion can influence recommendations.
3. Roadmap progression can influence mentor guidance.
4. Dashboard metrics can consume unified intelligence.
5. All systems share a common intelligence layer.
6. Architecture remains scalable for all engineering domains.
7. Existing UI and behavior remain unchanged.

IMPORTANT:

This is a synchronization and orchestration task only.

Focus on intelligence propagation, shared state, events, and selectors.

Do not generate UI.
Do not redesign anything.
Do not rebuild existing systems.